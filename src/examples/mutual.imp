#fatorial mutuamente recursivo

let var z = 1 in
   let rec fn f(x) =
        z := z * x
        g(x - 1)
    , rec fn g(y) = 
        if y > 1 then
            f(y)
        end
    , var w = 2 in
   g(5)

    